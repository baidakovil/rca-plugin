my hot-reloading system uses the AssemblyAttribute to mark assemblies that should be reloaded when their source code changes. This allows me to update parts of my application without restarting the entire application, which is especially useful during development. But AssemblyAttribute can be seen only from the code. can we embed it in the assembly metadata so that it can be accessed without loading the assembly? for example, from Properties window in Windows. Can we use Product version for inheriting the hash and folder Attributes? for example, as "{hash}.{folder}"?
