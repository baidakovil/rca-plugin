<UserControl x:Class="Rca.UI.ChatPanel.RcaDockablePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#F5F5F5">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Top Zone -->
            <RowDefinition Height="*"/>    <!-- Content -->
            <RowDefinition Height="92"/>   <!-- Input Area, fixed height for bottom -->
        </Grid.RowDefinitions>

        <!-- Fixed Top Zone -->
        <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#C8C8C8" BorderThickness="0,0,0,1" Height="40" Padding="0,0,10,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="1" Content="CHAT HISTORY" Margin="5,0,5,0" Padding="5" Width="100" Height="30" Background="#ECECEC" BorderBrush="#C8C8C8"/>
                <Button Grid.Column="2" Content="NEW CHAT" Margin="5,0,5,0" Padding="5" Width="100" Height="30" Background="#ECECEC" BorderBrush="#C8C8C8"/>
                <Button Grid.Column="3" Content="LOGS" Margin="5,0,5,0" Padding="5" Width="100" Height="30" Background="#ECECEC" BorderBrush="#C8C8C8" Command="{Binding ShowLogViewerCommand}"/>
            </Grid>
        </Border>

        <!-- Central Panel -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0" Background="#F5F5F5">
            <StackPanel Background="#F5F5F5">
                <TextBlock Text="Python Output" Margin="5" FontSize="14" FontWeight="Bold"/>
                <Expander Header="Code Snippet" Margin="10" Padding="3" Background="#ECECEC" BorderBrush="#C8C8C8" BorderThickness="1">
                    <StackPanel Background="#F5F5F5">
                        <TextBlock Text="{Binding OutputText}" Margin="5" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
                        <Button Content="EXECUTE" HorizontalAlignment="Right" Margin="5" Padding="5" Background="#ECECEC" BorderBrush="#C8C8C8" Width="100" Height="30" Command="{Binding ExecutePythonCommand}"/>
                    </StackPanel>
                </Expander>
            </StackPanel>
        </ScrollViewer>

        <!-- Bottom Input Area: TextBox fills, Button overlays bottom-right -->
        <Grid Grid.Row="2" Margin="10">
            <TextBox AcceptsReturn="True" VerticalScrollBarVisibility="Visible" Margin="0" FontSize="12" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" 
                     HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#F5F5F5" BorderBrush="#C8C8C8"/>
            <Button Content="SEND" Padding="0" Background="#ECECEC" BorderBrush="#C8C8C8" Width="100" Height="30" 
                    HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10,10,30,10" Command="{Binding ClickCommand}"/>
        </Grid>
    </Grid>
</UserControl>
